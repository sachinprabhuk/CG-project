#include "../headers/constants.h"

const int W_WIDTH = 600;
const int W_HEIGHT = 400;
const float PI = 3.1416;
const float TWO_PI = 2 * 3.1416;
const float CIRCLE_GROWTH_SPEED = 0.15;
const float MOUSE_DIST_THRESHOLD = 100;
const float PULL_BACK_DRAG = 1 / (float)28;
const float REPELL_DRAG = 1 / (float)15;
const Color COLORS[5] = {
		{255 / (float)255, 255 / (float)255, 5 / (float)255},
		{0 / (float)255, 41 / (float)255, 250 / (float)255},
		{61 / (float)255, 61 / (float)255, 61 / (float)255},
		{141 / (float)255, 7 / (float)255, 246 / (float)255},
		{166 / (float)255, 171 / (float)255, 171 / (float)255},
};
